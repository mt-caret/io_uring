(executables
  (names nop)
  (modules nop)
  (libraries core io_uring)
  (preprocess (pps ppx_jane)))

(executables
  (names cp)
  (modules cp)
  (libraries core io_uring)
  (preprocess (pps ppx_jane)))
